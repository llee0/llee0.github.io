title: LZL Engineering
logo: 
email: info@lzlengineering.com
description: >- # this means to ignore newlines until "baseurl:"
  Fluidized Bed Particle Processing Solutions
show_downloads:
google_analytics:
domain: lzlengineering.com
url: "https://lzlengineering.com" # the base hostname & protocol for your site, e.g. http://example.com
baseurl: "/docs" # the subpath of your site, e.g. /blog
# twitter_username: jekyllrb
# github_username:  jekyll
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
- .sass-cache/
- .jekyll-cache/
- gemfiles/
- Gemfile
- Gemfile.lock
- node_modules/
- vendor/bundle/
- vendor/cache/
- vendor/gems/
- vendor/ruby/
